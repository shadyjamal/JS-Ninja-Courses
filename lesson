primitive types
    stored on the stack
referencne types
    stored on the heap with a pointer to the object stored in stack

Async JS
    Governs how we perform tasks which take some time to complete () getting data from db or APIs
    Async code is code that start now and may finish later

HTTP Requests
- Make http requests to get data from another server
- we make these requests to API endpoints and get JSON DATA
